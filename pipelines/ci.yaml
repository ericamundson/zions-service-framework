trigger: none

pool:
  name: Default
  demands:
  - agent.name -equals bagent

steps:
- task: Gradle@2
  displayName: 'gradlew clean build'
  inputs:
    options: '--no-scan -Pqueue=default -Pcontext=all'
    tasks: 'clean build'
    publishJUnitResults: false